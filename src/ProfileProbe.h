#pragma once

// The W1 field harness, successor to phase 0's ChargenProbe on the same
// sacrificed key: press once to CAPTURE the player into the profile "F11
// Look" (face included), press again to APPLY that profile whole. A SPIKE,
// like every probe before it: it dies with the Looks page (W2), which is the
// real trigger for both halves.
namespace OS::ProfileProbe {

    void OnKey();

}  // namespace OS::ProfileProbe
